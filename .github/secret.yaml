name: Secret Finder
on: workflow_dispatch
jobs:
  name: Expose secret
  runs-on: ubuntu-latest
  steps:
    - name: My Secret Expose
      run: echo "The Secret Stored Inside MYSECRET is ${{secrets.MYSECRET}}"
